/* Copy to main/secrets.h and fill in. secrets.h is gitignored and must never
 * be committed, nor present in a tree used to cut a public release binary. */
#pragma once

#define TAPTALK_WIFI_SSID     "your-ssid"
#define TAPTALK_WIFI_PASSWORD "your-password"

/* Unused in chunk 1; the STT client lands in chunk 2. */
#define TAPTALK_OPENAI_API_KEY "sk-..."
